import latexify
import math

# latexify.get_latex obtains the underlying LaTeX expression directly.
def eq():
  return A_(0-0)/(0-1) (1-G(l^20)*e^(-l^2)*W/(2*E_p))^2 / (l^2 * (1-G(l^2)*e^(-l^2)*W/(2*E_p))^2)

print(latexify.get_latex(eq))
